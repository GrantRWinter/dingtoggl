class Admin::UsersController < ApplicationController

  def show
    # load the @user by params[:id]
    # load their entries into @entries to display in the view
    # the template will display the entries for just that user
  end

end
